# =============================================================================
#  VOICE_DIFFS.patch — the W7 anti-cringe edits to files that task did NOT own.
# =============================================================================
#  Three agents owned src/game/main.hml this wave and its md5 moved FOUR times
#  while the audit ran, so LINE NUMBERS IN THIS PATCH ARE ALREADY WORTHLESS.
#  It is included as the readable record of exactly what changes.
#
#  >>> APPLY IT WITH THE STRING-REPLACEMENT SCRIPT, NOT WITH `patch`. <<<
#      scratchpad/apply_voice.py performs the same 24 edits as EXACT STRING
#      REPLACEMENTS and asserts that each old text appears exactly once, so it
#      either lands correctly on a moved file or refuses and names the anchor.
#      A copy of that script is reproduced at the end of this file.
#
#  Generated against, and measured on, this snapshot:
#      src/game/main.hml    8dcf8af0324ebfceeff99288557b5571
#      src/game/client.hml  6af8b5b297936d7c65bc60f48c8af38b
#      src/art/hudgen.hml   09187344955de437692aaae19fc93c74
#      tools/probe_hud.hml  (unmodified at snapshot time)
#
#  MEASURED TOGETHER, paired A/B one apply apart, loadavg 4.11 / 3.74:
#      --selftest              40/40   ->  40/40
#      replay --verify   10000/10000 hash 16594734899016964914, cosmetics 0
#                                      ->  identical
#      HUD peak --demo 80        334   ->  330  of 360   (NET -4)
#      world triangles          1791   ->  1789 of 3000
#      fps / frames over 16.67  59.5 / 81 of 4765 -> 59.5 / 81 of 4763
#      CHARACTER panel           212   ->  300  of 360
#      TRADE panel               208   ->  216  of 360
#      probe_hud               16/16   ->  16/16 (red at 112>108 before D)
#      probe_npc / probe_teach 269/269, 87/87  ->  unchanged
#      ci_imports / ci_unbox    8/8 PASS, 0 boxed  ->  unchanged
#
#  Rationale for every edit: docs/design/VOICE.md sections 2, 3, 6 and 8.
# =============================================================================

--- a/src/game/client.hml
+++ b/src/game/client.hml
@@ -931,7 +931,14 @@
     let de: i32 = l.dir_events;
     if ((de & DIR_EV_HORN) != 0) {
         cl_sound(cl, CSND_HORN, 1.0, 0.0);
-        cl_banner(cl, "THE LAMPS ARE OUT");
+        // W7-VOICE: was "THE LAMPS ARE OUT", and it fired at the exact moment
+        // the player had just LIT one -- a banner contradicting the frame it is
+        // drawn over. It also repeated the intro card ("THE LAMPS WENT OUT ON A
+        // TUESDAY") sixty seconds later, meaning something else. What actually
+        // happened is the payoff to the warning under the light prompt, "IT'S
+        // GOING TO GET THEIR ATTENTION", so the banner is now that sentence
+        // coming true. 56 -> 44 triangles.
+        cl_banner(cl, "THEY HEARD IT");
     }
     if ((de & DIR_EV_WAVE_START) != 0) {
         cl_banner(cl, `WAVE ${l.wave + 1}`);
@@ -1117,7 +1124,21 @@
                  1.1, 0.24, 0.06, 255, 176, 96, 235);
         i++;
     }
-    cl_card(cl, "TINKER SMG - AT THE BENCH");
+    // >>> W7-VOICE REMOVED A CARD HERE, AND THE REASON IS THAT IT WAS A LIE.
+    // <<<  It read "TINKER SMG - AT THE BENCH" and fired on the first lantern.
+    // Three things wrong with it, in order of severity:
+    //   1. THE BENCH IS MABEL'S, and at 0:30 there is no Village, no Mabel and
+    //      no bench. The player is told where to collect a gun roughly twenty
+    //      five minutes before the place exists.
+    //   2. THE TINKER IS A CITY GUN. weapons.hml's village row is
+    //      { BLACKTHORN, KESTREL, BELLOWS, NETTLE } and the Tinker is not in
+    //      it, so even once the bench exists this row is not on it.
+    //   3. THE BEAT ALREADY HAS A CARD. Lighting the lantern grants the FLARE,
+    //      and `cl_apply_w6`'s sp_have edge fires `cl_card(client_spell_name)`
+    //      on the same tick. Two cards competed for one 80-triangle slot and
+    //      the untrue one was drawn first.
+    // 80 triangles returned to the busiest moment in the first minute, and the
+    // card that survives says FLARE, which is a thing the player now has.
 }
 
 fn cl_on_beat(p_cl: object, p_beat: i32) {
@@ -1134,7 +1155,10 @@
         cl_sound(cl, CSND_WAVECLEAR, 0.9, 0.0);
     }
     if (b == SV_BEAT_PATH) {
-        cl_toast(cl, "A PATH OF LIGHT");
+        // W7-VOICE: was "A PATH OF LIGHT" -- a phrase, not a sentence, naming
+        // nothing the player can point at or act on. What the PATH beat does is
+        // put more unlit lanterns in the world. Same 48 triangles, one fact.
+        cl_toast(cl, "MORE LANTERNS");
     }
 }
 
@@ -2047,8 +2071,36 @@
 // These are HUD strings and nothing else reads them; `weapons.hml` owns ids
 // and numbers and deliberately owns no text at all (it is a sim module and a
 // string is not simulation state).
-let g_cl_wpn_name: array = ["SPARROW", "TINKER", "BELLOWS", "LNGSHDW",
-                            "KESTREL", "EMBRLNC", "BLKTHRN", "NETTLE"];
+// >>> W7-VOICE: THREE OF THESE WERE NOT NAMES, THEY WERE NUMBER PLATES. <<<
+// The clamp is `clampi(cap - used, 0, 28)` at main.hml's ammo plate -- 28
+// triangles, SEVEN GLYPHS at 4 each -- and it is a TRIANGLE limit, not a pixel
+// one: the slot is 78 px wide and "BLACKTHORN" is 41 px. So three ten-letter
+// names were disemvowelled to fit a budget, and the frame showed "BLKTHRN 5/64"
+// beside "KESTREL 6/12" in the same 78 px slot, one a word and one a licence
+// plate. CLAUDE.md 9.1's test answers itself here: a 1998 art director would
+// have renamed the gun.
+//   LONGSHADOW -> HERON    joins SPARROW and KESTREL. A heron is long, still,
+//                          patient and strikes at range: that IS a 75 RPM
+//                          semi-auto marksman rifle. 20 of 28.
+//   EMBERLANCE -> STOKER   and this one also fixes a THREE-WAY COLLISION --
+//                          EMBERLANCE is a SPELL (g_cl_spell_name[2]) and LANCE
+//                          is another one. spells.hml's own header says the
+//                          fire blast MOVED OUT of this gun's slot into the
+//                          spell, so the spell owns the name and the gun was
+//                          left holding a fossil. A stoker feeds a furnace
+//                          continuously, which is what an LMG does, and it is a
+//                          municipal job title in a world of lamplighters.
+//                          24 of 28.
+//   BLACKTHORN -> THORN    Mabel THORN forges it at her bench. Guns are named
+//                          after whoever made them -- Mauser, Garand, Mosin --
+//                          and in this world that is one woman with a shed.
+//                          LORE.md 10.7: "agricultural equipment that got a
+//                          promotion". 20 of 28.
+// Every one of the eight now has MARGIN inside the clamp instead of sitting on
+// it. The sim ids (WPN_BLACKTHORN et al.) are untouched: these are HUD strings
+// and weapons.hml deliberately owns no text.
+let g_cl_wpn_name: array = ["SPARROW", "TINKER", "BELLOWS", "HERON",
+                            "KESTREL", "STOKER", "THORN", "NETTLE"];
 let g_cl_wpn_bit: array = [1, 2, 4, 8, 16, 32, 64, 128];
 
 export fn client_weapon_name(p_w: i32): string {
@@ -2138,13 +2190,39 @@
 //                                Lantern's WICK, which is LORE.md's own word
 //      "BOLT-ACTION" -> "BOLT"   unambiguous next to REVOLVER / PUMP / PIPE
 //      "MANUFACTURED" -> "SPARROW"      it is the gun's actual name
+// W7-VOICE, two words out of sixteen. The other fourteen are exactly right --
+// BRASS, BED, SOUP, PACK, MAP are concrete nouns a person would say -- and the
+// note above about BANDOLIER -> BELT is the model for how to shorten one.
+//   REROLL -> REFIT   LORE.md 10.6: "Nobody explains the mechanics. No NPC has
+//                     ever said 'radiant damage'." REROLL is a dice word in a
+//                     world with no dice. A gunsmith REFITS a gun. Same row,
+//                     same price, 28 -> 24 triangles.
+//   WICK   -> TRIM    the row raises the Great Lantern's wick -- but WICK is
+//                     also the NAME OF THE MAN SELLING IT, and the panel header
+//                     three rows up reads "WICK   LANTERN". Trimming a wick is
+//                     what a lamplighter does to make a lamp burn better, it is
+//                     the correct trade verb, and it is not somebody's name.
+//   "GUN" (row 0) IS DEAD and is left alone: `cl_offer_name` intercepts kind 0
+//   and returns `g_cl_vw_name[arg]`, so this entry has never been drawn. Named
+//   rather than quietly deleted -- it is not this task's call to change a table
+//   length.
 let g_cl_off_name: array = [
     "GUN", "SPARROW", "BRASS", "REPAIR",
-    "BELT", "REROLL", "BED", "PACK",
-    "SOUP", "WICK", "LAMP KIT", "TRAVEL",
+    "BELT", "REFIT", "BED", "PACK",
+    "SOUP", "TRIM", "LAMP KIT", "TRAVEL",
     "WORK", "MAP", "ALMANAC", "CHARM"
 ];
-let g_cl_vw_name: array = ["BOLT", "REVOLVER", "PUMP", "PIPE SMG"];
+// >>> W7-VOICE: YOU BOUGHT A "PUMP" AND YOU WERE HANDED A "BELLOWS". <<<
+// Photographed at 320x180 (scratchpad/vpanel/ns_21..26): Mabel's panel offered
+// PUMP 65, and the ammo plate two seconds later read BELLOWS. All four village
+// guns had TWO unrelated names -- one at the point of purchase, one in your
+// hands -- and no string anywhere connected them. That is not a tone problem,
+// it is a comprehension bug, and it is the worst thing this audit found.
+// The panel now says what the gun is CALLED; what KIND it is, the player learns
+// from firing it, which is the only way anybody ever learns that anyway.
+// Measured through the real font: the dearest row goes 44 -> 40 triangles
+// (REVOLVER 120 -> KESTREL 120), so the panel's worst window got CHEAPER.
+let g_cl_vw_name: array = ["THORN", "KESTREL", "BELLOWS", "NETTLE"];
 
 // THE ROW, AS THE PLAYER READS IT: a name and what it costs.
 //
@@ -2236,13 +2314,39 @@
 let g_cl_dir_name: array = ["NORTH", "NORTHEAST", "EAST", "SOUTHEAST",
                             "SOUTH", "SOUTHWEST", "WEST", "NORTHWEST"];
 
+// How many daily lines this file actually ships per villager. It MUST equal
+// `NPC_LINES` in src/sim/npc.hml, which is what the sim selects across -- if
+// this is smaller the top lines are unreachable, and if it is larger the clamp
+// below silently folds the extras onto the last one. It is a local constant and
+// not an import because the import wall runs the other way: the HUD may read
+// the sim, and this table is the HUD's.
+let NPC_LINES_MAX: i32 = 6;
+
 export fn client_talk_line(p_who: i32, p_topic: i32, p_arg: i32): string {
     let t: i32 = p_topic;
     let a: i32 = p_arg;
     if (t < 0) { let none: string = ""; return none; }
     if (t == 0) {                        // NPC_T_GREET
-        let s: string = "YOU CAME BACK.";
-        return s;
+        // >>> "YOU CAME BACK." WAS SAID TO PEOPLE YOU HAD NEVER MET. <<<
+        // npc.hml 18 step 1 fires NPC_T_GREET on `met[w] == 0` and on nothing
+        // else -- it is the FIRST-MEETING topic, and it is the only topic that
+        // can never fire twice. Photographed: walk up to Mabel for the first
+        // time in your life and she says "You came back." It also spent the
+        // sentence this whole game is about ("the town survived because you
+        // came back", DIRECTION.md 6.4) on hello.
+        // Five lines now, one each, straight out of LORE.md 8's voices, all
+        // measured under the 96-triangle talk-line clamp (worst 88).
+        // "YOU CAME BACK." wants a RETURNING topic and npc.hml already tracks
+        // `talkday` per person, so the data is there; adding the topic needs an
+        // owner of both files and is REPORTED in docs/design/VOICE.md 6 rather
+        // than half-done here.
+        let g: i32 = clampi(a, 0, 4);
+        if (g == 0) { let s0: string = "YOU'LL BE THE LAMPLIGHTER."; return s0; }
+        if (g == 1) { let s1: string = "SIT DOWN. YOU'RE FILTHY."; return s1; }
+        if (g == 2) { let s2: string = "MM. YOU'RE ON THE ROTA."; return s2; }
+        if (g == 3) { let s3: string = "OH, MARVELLOUS. A NEW ONE."; return s3; }
+        let s4: string = "I KNEW SOMEONE WOULD COME.";
+        return s4;
     }
     if (t == 1) {                        // NPC_T_BROWNOUT
         let s: string = "THE LIGHTS ARE GUTTERING.";
@@ -2277,12 +2381,97 @@
         let s: string = "STAY IN THE LIGHT TONIGHT.";
         return s;
     }
-    // NPC_T_DAILY — the one line per in-game day, per person.
+    if (t == 10) {                       // NPC_T_KIT
+        // THE SEAM main.hml ASKED FOR, CLOSED. Its own comment: ">>> THIS
+        // BELONGS IN `client_talk_line` AS `if (t == 10)`, one line, the next
+        // time that file is owned. <<<" This is that time. `p_arg` is the ring
+        // in whole metres, so the line stays true as the town promotes and the
+        // ring goes 64 -> 120 -> 200. 76-80 of the 96 clamp.
+        // The space in "${a} M" is dropped: every other number on this HUD is
+        // "112M TO THE REACH", and three instruments saying the same word in
+        // the same shape is one lesson.
+        let s: string = `INSIDE ${a}M IT FEEDS US.`;
+        return s;
+    }
+    // ---- NPC_T_DAILY -----------------------------------------------------
+    //
+    // >>> `NPC_LINES = 6` HAS BEEN SELECTED, HASHED AND THROWN AWAY SINCE W6.
+    // <<<  npc.hml 18 step 10 computes a flavour index into `p_out[1]` and this
+    // arm read `p_who` and IGNORED `p_arg` entirely, so every villager had
+    // exactly ONE daily line, said on every in-game day, forever. GAME_DESIGN
+    // 5.7's "one new line per in-game day" has never once happened.
+    //
+    // AND ONE OF THE FIVE WAS ALSO TRUNCATING. Priced through the real font,
+    // "TWO FINGERS SHORT, STILL STRAIGHT." is 120 triangles against this draw
+    // site's 96-triangle clamp: `font_text_shadowed` cut it at 24 ink glyphs
+    // and Mabel's signature line rendered as "TWO FINGERS SHORT, STILL STR".
+    // Nobody had measured it because it took two in-game days and a Village to
+    // reach. Every one of the thirty below is under 96; the worst is 96/96.
+    //
+    // THE REPETITION IS THE POINT OF THIS FIX AND NOT THE COUNT. LORE.md 8 on
+    // Mabel's fingers: "She tells you this exactly once ... Do not soften her
+    // and do not give her a second scene about it. One scene. Then the
+    // weather." The shipping game told you every day. A line said too often is
+    // not merely wasted, it is RETROSPECTIVELY DAMAGED -- "Soup's warm past
+    // midnight" is the best sentence in this game exactly once.
+    //
+    // FORM: LORE.md 12.1 -- never about a quest, never about the player's
+    // stats, about the weather, food, a small worry, or a thing they noticed.
+    // Register: LORE.md 10 -- civic not epic, short sentences, ordinary words,
+    // understatement carries the grief. The style guide is docs/design/VOICE.md
+    // 5 and it is four rules long on purpose.
     let w: i32 = clampi(p_who, 0, 4);
-    if (w == 0) { let s: string = "TWO FINGERS SHORT, STILL STRAIGHT."; return s; }
-    if (w == 1) { let s: string = "SOUP'S WARM PAST MIDNIGHT."; return s; }
-    if (w == 2) { let s: string = "MIND THE WICK, NOT THE FLAME."; return s; }
-    if (w == 3) { let s: string = "I'VE MAPPED WHAT I CAN SEE."; return s; }
-    let s2: string = "I FOUND A THING. HONEST.";
+    let d: i32 = clampi(a, 0, NPC_LINES_MAX - 1);
+    if (w == 0) {                        // MABEL -- gunsmith, dry, a tool she
+        // has repaired. Six about the gun and the weather; the fingers exactly
+        // once, in a form that fits (was 120 of 96, is 92).
+        if (d == 0) { let s: string = "WIND'S EAST. AIM LEFT."; return s; }
+        if (d == 1) { let s: string = "BRING IT BACK UNBROKEN."; return s; }
+        if (d == 2) { let s: string = "I'VE BURIED BETTER GUNS."; return s; }
+        if (d == 3) { let s: string = "THE SPRING'S TIRED. SO AM I."; return s; }
+        if (d == 4) { let s: string = "DON'T BLAME THE GUN."; return s; }
+        let s5: string = "TWO SHORT. STILL STRAIGHT.";
+        return s5;
+    }
+    if (w == 1) {                        // ODO -- everything is about food and
+        // none of it is about food. LORE.md 10.3 names "I kept a bowl" the
+        // saddest line in the game; it is here once, not daily.
+        if (d == 0) { let s: string = "SOUP'S WARM PAST MIDNIGHT."; return s; }
+        if (d == 1) { let s: string = "I KEPT A BOWL. I ALWAYS DO."; return s; }
+        if (d == 2) { let s: string = "YOU'VE GOT MUD ON YOU."; return s; }
+        if (d == 3) { let s: string = "THE BEDS TOOK BADLY. DRY."; return s; }
+        if (d == 4) { let s: string = "EAT SOMETHING. PLEASE."; return s; }
+        let s5: string = "SOUP KEEPS. THAT'S ALL.";
+        return s5;
+    }
+    if (w == 2) {                        // WICK -- aphorisms he refuses to
+        // explain, and the paperwork of an apocalypse that was a maintenance
+        // failure (LORE.md 2). He never once says what is at the bottom.
+        if (d == 0) { let s: string = "MIND THE WICK, NOT THE FLAME."; return s; }
+        if (d == 1) { let s: string = "MM. BACK BY THE SECOND HOUR."; return s; }
+        if (d == 2) { let s: string = "OIL WAS SHORT AGAIN."; return s; }
+        if (d == 3) { let s: string = "A LAMP DOESN'T ARGUE."; return s; }
+        if (d == 4) { let s: string = "FOUR HUNDRED YEARS OF IT."; return s; }
+        let s5: string = "MM. OFF YOU GO.";
+        return s5;
+    }
+    if (w == 3) {                        // CONNIE -- thrilled by danger she has
+        // personally never attended, and a bad writer in an endearing way.
+        if (d == 0) { let s: string = "I'VE MAPPED WHAT I CAN SEE."; return s; }
+        if (d == 1) { let s: string = "THE THIRD RING IS WRONG."; return s; }
+        if (d == 2) { let s: string = "REDREW IT TWICE. THRILLING."; return s; }
+        if (d == 3) { let s: string = "I'D COME. MY KNEE, THOUGH."; return s; }
+        if (d == 4) { let s: string = "SOMEONE MUST WRITE IT DOWN."; return s; }
+        let s5: string = "MY INK FROZE. AGAIN.";
+        return s5;
+    }
+    // PIP -- the lies get MORE specific the less true they are, and the last
+    // one is the ask LORE.md 8 says he makes once and you cannot grant.
+    if (d == 0) { let s: string = "I FOUND A THING. HONEST."; return s; }
+    if (d == 1) { let s: string = "I COULD CARRY MORE THAN YOU."; return s; }
+    if (d == 2) { let s: string = "I'VE GOT A SYSTEM."; return s; }
+    if (d == 3) { let s: string = "I'VE SEEN ONE. UP CLOSE."; return s; }
+    if (d == 4) { let s: string = "I SAID NOTHING. I AM BRAVE."; return s; }
+    let s2: string = "TAKE ME OUT THERE. YOU COULD.";
     return s2;
 }
--- a/src/game/main.hml
+++ b/src/game/main.hml
@@ -412,7 +412,10 @@
 //  0.  FIXED SHAPE
 // ===========================================================================
 
-let g_VERSION: string = "NIGHTSHADE W6 - the first minute, and the town you carry it home to";
+// W7-VOICE: the wave tag came out. It was two waves stale, it is an internal
+// build id in a line a player reads at boot, and the subtitle is the best
+// one-sentence statement of this game that exists anywhere in the repo.
+let g_VERSION: string = "NIGHTSHADE - the first minute, and the town you carry it home to";
 
 let g_CHUNK_Q: i32 = 8;                 // quads per terrain chunk edge
 let g_QUAD_M: f64 = 4.0;                // metres per terrain quad
@@ -1564,12 +1567,34 @@
         // on a 1.30 s line is a quarter of it spent lying.
         if (g_teach_verdict == TEACH_V_INSIDE) {
             g_teach_plants_in++;
-            g_l_verdict = "THE GRID TAKES IT";
+            // >>> W7-VOICE: WAS "THE GRID TAKES IT". <<<  Three faults, and
+            // the third is the one that decides it:
+            //   1. "GRID" APPEARS IN NO OTHER STRING IN THIS GAME. It is in
+            //      LORE.md 2 and in npc.hml's comments and nowhere a player can
+            //      read it, so the single most important confirmation in the
+            //      second verb was phrased in a noun nothing had taught.
+            //   2. "TAKES IT" reads as CONFISCATES at least as easily as it
+            //      reads as accepts. A player who plants a lantern and is told
+            //      something took it has been told they lost something.
+            //   3. THE FAILURE LINE WAS BETTER WRITTEN THAN THE SUCCESS LINE.
+            //      The other arm is "25M OUTSIDE THE REACH" -- concrete,
+            //      numeric, in the taught vocabulary -- and the reward was an
+            //      abstraction. That is exactly backwards.
+            // "THE TOWN GETS IT" names the beneficiary, needs no new word, and
+            // is what the fantasy is actually about. 56 -> 52 triangles.
+            g_l_verdict = "THE TOWN GETS IT";
         } else {
             g_teach_plants_out++;
             let gap: i32 = i32(g_teach_d - settle_ring_m(client_settle_tier_ever(g_cl))) + 1;
             if (gap < 1) { gap = 1; }
-            g_l_verdict = `${gap}M OUTSIDE THE REACH`;
+            // W7-VOICE, and this one is only visible in the FRAME. At the
+            // moment this draws, the prompt 40 px below it reads "25M TO THE
+            // REACH" -- the same number, the same fact, two prepositions,
+            // photographed together in scratchpad/vgrid/ns_33. "TO" and
+            // "OUTSIDE" read as a contradiction at 320x180 even though both are
+            // true. "SHORT OF" is a verdict rather than a second measurement,
+            // keeps the taught noun, and costs the same 72 triangles.
+            g_l_verdict = `${gap}M SHORT OF THE REACH`;
         }
     }
     g_teach_placed = placed;
@@ -2159,7 +2184,17 @@
 // no longer reachable for this string. It is a BUDGET, not a length limit — the
 // cap below still truncates rather than overspends if a frame is genuinely full.
 let g_TB_CARD: i32 = 80;
-let g_TB_PROMPT: i32 = 108;
+// W7-VOICE: 108 -> 112. `IT'S GOING TO GET THEIR ATTENTION` cost 108 with an
+// apostrophe that drew nothing, which is to say it was sitting EXACTLY on its
+// clamp only because a glyph was missing. Give FONT_MICRO the apostrophe (see
+// src/art/hudgen.hml) and the line is 112, and `font_text_shadowed` would drop
+// its last glyph on every lantern hold in the game. probe_hud section H caught
+// it on the first run. Budgets must be multiples of 4 to be spent exactly (see
+// g_TB_CARD above); 112 is, and it is the string's whole cost.
+// Measured: HUD peak over `--demo 80 --seed 1337` is 330 of 360 WITH this and
+// the apostrophe, against 334 without either, so it is paid for several times
+// over by the rest of this pass.
+let g_TB_PROMPT: i32 = 112;
 let g_TB_DBG: i32 = 120;
 // The damage number is at most 3 FONT_BIG digits (`cl.dmgnum` is clamped to
 // 999) and a FONT_BIG glyph is 4 triangles with its mandatory shadow, so 12 is
@@ -2504,7 +2539,7 @@
         n = n + hud_help_row(1, "T", "TRADE", p_budget - n);
         n = n + hud_help_row(2, "C", "CAST", p_budget - n);
         n = n + hud_help_row(3, "X", "NEXT", p_budget - n);
-        n = n + hud_help_row(4, "TAB", "SHEET", p_budget - n);
+        n = n + hud_help_row(4, "TAB", "STATS", p_budget - n);
         n = n + hud_help_row(5, "1-8", "GUNS", p_budget - n);
         n = n + hud_help_row(6, "Q", "AIM", p_budget - n);
         n = n + hud_help_row(7, "CTRL", "CROUCH", p_budget - n);
@@ -2669,7 +2704,11 @@
     let rows: i32 = client_ui_rows(g_cl);
 
     if (panel == UI_PANEL_CHAR) {
-        n = n + hud_panel_text(34.0, g_PANEL_Y + 8.0, "CHARACTER", g_UI_AMBER,
+        // W7-VOICE: was "CHARACTER", while the F1 controls card calls the same
+        // panel "SHEET". One thing, two names, exactly the defect the trade
+        // panel had with PUMP/BELLOWS. Both now say STATS, which is also what
+        // the five rows below are. 36 -> 20 triangles.
+        n = n + hud_panel_text(34.0, g_PANEL_Y + 8.0, "STATS", g_UI_AMBER,
                                clampi(p_budget - n, 0, 40));
         let pts: i32 = client_points(g_cl);
         let pc: i32 = g_UI_DIM;
@@ -2679,8 +2718,18 @@
         for (let i = 0; i < 5; i++) {
             let sel: i32 = 0;
             if (i == row) { sel = 1; }
+            // W7-VOICE: was `client_stat_short(i)` -- HND / CAP / LAN / VIT
+            // / ATT, five three-letter stubs in a panel with 250 px of empty
+            // room to the right of every one of them (photographed,
+            // scratchpad/vpanel/ns_23). "LAN" is not a word. The full table
+            // ALREADY EXISTED in client.hml and nothing was calling it.
+            // Measured through the real font: the five labels go 60 -> 164
+            // triangles, taking the panel 212 -> 316 of the 360 cap, which
+            // still clears probe_hud's 36-triangle headroom rule at 44. It does
+            // make probe_hud's `g_PANEL_CHAR = 212` stale, and that file is not
+            // this task's -- see docs/design/VOICE.md 6.
             n = n + hud_panel_row(g_PANEL_Y + 26.0 + f64(i) * 14.0, sel,
-                                  client_stat_short(i),
+                                  client_stat_name(i),
                                   `${client_stat(g_cl, i)}`,
                                   g_UI_DIM, p_budget - n);
         }
@@ -3849,9 +3898,24 @@
     }
 
     // ---- the wave readout ------------------------------------------------
+    // W7-VOICE: was `WAVE 1  x0`, unconditionally. `x0` is a COUNT rendered
+    // as a MULTIPLIER -- "wave one, times zero" -- and it sat on screen reading
+    // zero for the whole gap between waves, which is the one state in which it
+    // has nothing to say. It now says nothing when the answer is none.
+    //
+    // >>> AND IT IS THE CHEAP VERSION ON PURPOSE. <<<  The first draft of this
+    // edit read `WAVE 1  3 LEFT`, which is better English and costs 40
+    // triangles against this line's 24. Measured on a real `--demo 80`, that
+    // took the HUD peak 332 -> 354 of a 360 cap: SIXTEEN of the twenty-eight
+    // triangles left in the whole HUD, spent turning "x3" into "3 LEFT" on a
+    // line nobody has ever complained about. CLAUDE.md 3 is a hard rule and an
+    // anti-cringe pass does not get to spend the frame budget on taste. The
+    // version below is <= the old cost in EVERY state and still kills the `x0`.
     if (ws != DIR_IDLE && used + 44 <= cap) {
-        used = used + hud_text(4.0, 16.0,
-                               `WAVE ${client_wave(g_cl) + 1}  x${client_alive(g_cl)}`,
+        let alive: i32 = client_alive(g_cl);
+        let wl: string = `WAVE ${client_wave(g_cl) + 1}`;
+        if (alive > 0) { wl = `WAVE ${client_wave(g_cl) + 1}  x${alive}`; }
+        used = used + hud_text(4.0, 16.0, wl,
                                g_UI_DIM, clampi(cap - used, 0, 44));
     }
 
@@ -5189,10 +5253,10 @@
     // so a banner's LETTERS cost nothing and only its digits draw. The widest
     // banner the client can set is therefore `WAVE nn CLEARED`.
     g_hw_banner = "WAVE 99 CLEARED";
-    if (hw_cost_big("THE LAMPS ARE OUT") > hw_cost_big(g_hw_banner)) {
-        g_hw_banner = "THE LAMPS ARE OUT";
+    if (hw_cost_big("THEY HEARD IT") > hw_cost_big(g_hw_banner)) {
+        g_hw_banner = "THEY HEARD IT";
     }
-    g_hw_toast = "A PATH OF LIGHT";
+    g_hw_toast = "MORE LANTERNS";
     if (hw_cost_micro("POST SET") > hw_cost_micro(g_hw_toast)) { g_hw_toast = "POST SET"; }
     for (let v3 = 0; v3 < NPC_NAMED; v3++) {
         if (hw_cost_micro(client_npc_name(v3)) > hw_cost_micro(g_hw_toast)) {
@@ -5201,9 +5265,9 @@
     }
     // The two lines this file builds at 4 Hz, at their widest: three digits of
     // gap is 999 m, and the ring is never that wide.
-    g_hw_verdict = "999M OUTSIDE THE REACH";
-    if (hw_cost_micro("THE GRID TAKES IT") > hw_cost_micro(g_hw_verdict)) {
-        g_hw_verdict = "THE GRID TAKES IT";
+    g_hw_verdict = "999M SHORT OF THE REACH";
+    if (hw_cost_micro("THE TOWN GETS IT") > hw_cost_micro(g_hw_verdict)) {
+        g_hw_verdict = "THE TOWN GETS IT";
     }
     g_hw_reach = "999M TO THE REACH";
     if (hw_cost_micro("INSIDE THE REACH") > hw_cost_micro(g_hw_reach)) {
--- a/src/art/hudgen.hml
+++ b/src/art/hudgen.hml
@@ -104,7 +104,25 @@
 export let HUD_MICRO_H    = 6;
 export let HUD_MICRO_ADV  = 4;
 export let HUD_MICRO_LINE = 8;
-export let HUD_MICRO_N    = 50;
+// >>> W7-VOICE: 50 -> 51, AND THE 51st IS AN APOSTROPHE. <<<
+// A 320x180 screenshot of Mabel's first line read "MM. YOU RE ON THE ROTA."
+// The apostrophe was NOT in this font; `font.hml` advances the pen for an
+// unmapped codepoint and draws nothing, so EVERY contraction in EVERY line of
+// dialogue in this game has rendered as a hole since the day it was written.
+// "IT'S GOING TO GET THEIR ATTENTION" -- the sentence LORE.md 3 calls the
+// central bargain of the whole game, on screen for the full 2.5 s lantern hold
+// -- has always read "IT S GOING TO GET THEIR ATTENTION".
+// SAME CLASS AS "FONT_BIG HAS NO LETTERS", which hid the death banner for the
+// entire history of the project, and found the same way: by looking at the
+// picture. Adding the glyph is cheaper than writing around it, and writing
+// around it would mean a game whose people may not use contractions -- which
+// is most of LORE.md 10.4's register.
+// APPENDED, not inserted: glyph indices 0..49 and their ATLAS_HUD UVs do not
+// move. Costs 4 triangles (2 + the mandatory shadow) per contraction. The
+// worst affected line is `IT'S GOING TO GET THEIR ATTENTION` at 108 -> 112,
+// which needed main.hml's g_TB_PROMPT raised 108 -> 112 with it; probe_hud
+// section H went red on the first run and named that line by itself.
+export let HUD_MICRO_N    = 51;
 export let HUD_MICRO_COLS = 32;
 
 // FONT_BIG — 8x12 cell, 7-px ink with a uniform 2-px stroke, 14-px line pitch.
@@ -380,7 +398,12 @@
     if (i == 46) { return [0b1000, 0b0100, 0b0010, 0b0100, 0b1000, 0b0000]; }  // >
     if (i == 47) { return [0b0100, 0b1010, 0b0100, 0b0000, 0b0000, 0b0000]; }  // degree  U+00B0
     if (i == 48) { return [0b1010, 0b1110, 0b1110, 0b0100, 0b0000, 0b0000]; }  // heart   U+2665
-    return [0b0010, 0b0110, 0b1100, 0b0100, 0b1000, 0b0000];                   // bolt    U+2301
+    if (i == 49) { return [0b0010, 0b0110, 0b1100, 0b0100, 0b1000, 0b0000]; }  // bolt    U+2301
+    // ' apostrophe.  One column, the top two rows of the 4x6 cell, so it sits
+    // at cap height where the eye expects it in an ALL-CAPS font.  Same 4-px
+    // advance as every other glyph, so no line's WIDTH changes -- only its ink,
+    // and only where a contraction already was.  W7-VOICE.
+    return [0b0100, 0b0100, 0b0000, 0b0000, 0b0000, 0b0000];                   // '  U+0027
 }
 
 // Codepoint of micro glyph slot `i`.  Hemlock strings index by CODEPOINT, not
@@ -401,7 +424,8 @@
     if (i == 46) { let c: i32 = 62; return c; }                        // >
     if (i == 47) { let c: i32 = 176; return c; }                       // U+00B0 degree
     if (i == 48) { let c: i32 = 9829; return c; }                      // U+2665 heart
-    let c2: i32 = 8961; return c2;                                     // U+2301 bolt
+    if (i == 49) { let c: i32 = 8961; return c; }                      // U+2301 bolt
+    let c3: i32 = 39; return c3;                                       // ' U+0027  W7-VOICE
 }
 
 fn micro_adv(i: i32): i32 {
--- a/tools/probe_hud.hml
+++ b/tools/probe_hud.hml
@@ -244,8 +244,17 @@
     print(`   THE SET THAT MAY NEVER YIELD, minus prompts       ${g_never_yield}`);
 
     // ---- the prompts.  Two of them, and only one can be near you --------
-    let p1: i32 = line_trunc("HOLD [E] line 1", cost_micro("HOLD [E] - LIGHT THE LANTERN"), 108);
-    let p2: i32 = line_trunc("HOLD [E] line 2", cost_micro("IT'S GOING TO GET THEIR ATTENTION"), 108);
+    // W7-VOICE: 108 -> 112, tracking main.hml's `g_TB_PROMPT`.
+    // >>> AND THIS IS A STALE LITERAL, WHICH IS WHY IT NEEDED CHANGING BY HAND.
+    // <<<  `g_TB_PROMPT` lives in src/game/main.hml and is not exported, so this
+    // file cannot import the number it is asserting against and carries its own
+    // copy instead. That is the exact shape of the bug CLAUDE.md records twice
+    // already ("the divisor now derives from g_RES_H instead of the literal 2.4
+    // that hid the regression"). THE RIGHT FIX IS TO EXPORT g_TB_PROMPT and
+    // import it here; that is a main.hml change and is reported in
+    // docs/design/VOICE.md 6 rather than smuggled into a probe.
+    let p1: i32 = line_trunc("HOLD [E] line 1", cost_micro("HOLD [E] - LIGHT THE LANTERN"), 112);
+    let p2: i32 = line_trunc("HOLD [E] line 2", cost_micro("IT'S GOING TO GET THEIR ATTENTION"), 112);
     let e_prompt: i32 = 2 + p1 + p2 + 2 + 2;                 // plate, 2 lines, charge bar
     let t1: i32 = line_trunc("[E] MABEL", cost_micro("[E] MABEL"), 48);
     let t2: i32 = line_trunc("[T] TRADE", cost_micro("[T] TRADE"), 36);

# =============================================================================
#  THE APPLY SCRIPT (preferred over `patch`) — verbatim.
#  Run:  python3 apply_voice.py /home/nbeerbower/Projects/nightshade
# =============================================================================
# #!/usr/bin/env python3
# # apply_voice.py — the W7 anti-cringe edits to files this task does NOT own.
# # Exact string replacements, not line-number patches, because three agents were
# # editing main.hml while this ran (its md5 moved twice in one session).
# # Every replacement asserts its OLD text is present exactly once.
# import sys, os
# 
# ROOT = sys.argv[1] if len(sys.argv) > 1 else "."
# 
# CLIENT = []
# MAIN = []
# 
# # ---------------------------------------------------------------- client.hml
# # V1  the three disemvowelled guns become words
# CLIENT.append((
# '''let g_cl_wpn_name: array = ["SPARROW", "TINKER", "BELLOWS", "LNGSHDW",
#                             "KESTREL", "EMBRLNC", "BLKTHRN", "NETTLE"];''',
# '''// >>> W7-VOICE: THREE OF THESE WERE NOT NAMES, THEY WERE NUMBER PLATES. <<<
# // The clamp is `clampi(cap - used, 0, 28)` at main.hml's ammo plate -- 28
# // triangles, SEVEN GLYPHS at 4 each -- and it is a TRIANGLE limit, not a pixel
# // one: the slot is 78 px wide and "BLACKTHORN" is 41 px. So three ten-letter
# // names were disemvowelled to fit a budget, and the frame showed "BLKTHRN 5/64"
# // beside "KESTREL 6/12" in the same 78 px slot, one a word and one a licence
# // plate. CLAUDE.md 9.1's test answers itself here: a 1998 art director would
# // have renamed the gun.
# //   LONGSHADOW -> HERON    joins SPARROW and KESTREL. A heron is long, still,
# //                          patient and strikes at range: that IS a 75 RPM
# //                          semi-auto marksman rifle. 20 of 28.
# //   EMBERLANCE -> STOKER   and this one also fixes a THREE-WAY COLLISION --
# //                          EMBERLANCE is a SPELL (g_cl_spell_name[2]) and LANCE
# //                          is another one. spells.hml's own header says the
# //                          fire blast MOVED OUT of this gun's slot into the
# //                          spell, so the spell owns the name and the gun was
# //                          left holding a fossil. A stoker feeds a furnace
# //                          continuously, which is what an LMG does, and it is a
# //                          municipal job title in a world of lamplighters.
# //                          24 of 28.
# //   BLACKTHORN -> THORN    Mabel THORN forges it at her bench. Guns are named
# //                          after whoever made them -- Mauser, Garand, Mosin --
# //                          and in this world that is one woman with a shed.
# //                          LORE.md 10.7: "agricultural equipment that got a
# //                          promotion". 20 of 28.
# // Every one of the eight now has MARGIN inside the clamp instead of sitting on
# // it. The sim ids (WPN_BLACKTHORN et al.) are untouched: these are HUD strings
# // and weapons.hml deliberately owns no text.
# let g_cl_wpn_name: array = ["SPARROW", "TINKER", "BELLOWS", "HERON",
#                             "KESTREL", "STOKER", "THORN", "NETTLE"];'''))
# 
# # V2  the trade panel sold four guns under names the HUD never uses
# CLIENT.append((
# '''let g_cl_vw_name: array = ["BOLT", "REVOLVER", "PUMP", "PIPE SMG"];''',
# '''// >>> W7-VOICE: YOU BOUGHT A "PUMP" AND YOU WERE HANDED A "BELLOWS". <<<
# // Photographed at 320x180 (scratchpad/vpanel/ns_21..26): Mabel's panel offered
# // PUMP 65, and the ammo plate two seconds later read BELLOWS. All four village
# // guns had TWO unrelated names -- one at the point of purchase, one in your
# // hands -- and no string anywhere connected them. That is not a tone problem,
# // it is a comprehension bug, and it is the worst thing this audit found.
# // The panel now says what the gun is CALLED; what KIND it is, the player learns
# // from firing it, which is the only way anybody ever learns that anyway.
# // Measured through the real font: the dearest row goes 44 -> 40 triangles
# // (REVOLVER 120 -> KESTREL 120), so the panel's worst window got CHEAPER.
# let g_cl_vw_name: array = ["THORN", "KESTREL", "BELLOWS", "NETTLE"];'''))
# 
# # V3  two offer labels that speak in game-system words
# CLIENT.append((
# '''let g_cl_off_name: array = [
#     "GUN", "SPARROW", "BRASS", "REPAIR",
#     "BELT", "REROLL", "BED", "PACK",
#     "SOUP", "WICK", "LAMP KIT", "TRAVEL",
#     "WORK", "MAP", "ALMANAC", "CHARM"
# ];''',
# '''// W7-VOICE, two words out of sixteen. The other fourteen are exactly right --
# // BRASS, BED, SOUP, PACK, MAP are concrete nouns a person would say -- and the
# // note above about BANDOLIER -> BELT is the model for how to shorten one.
# //   REROLL -> REFIT   LORE.md 10.6: "Nobody explains the mechanics. No NPC has
# //                     ever said 'radiant damage'." REROLL is a dice word in a
# //                     world with no dice. A gunsmith REFITS a gun. Same row,
# //                     same price, 28 -> 24 triangles.
# //   WICK   -> TRIM    the row raises the Great Lantern's wick -- but WICK is
# //                     also the NAME OF THE MAN SELLING IT, and the panel header
# //                     three rows up reads "WICK   LANTERN". Trimming a wick is
# //                     what a lamplighter does to make a lamp burn better, it is
# //                     the correct trade verb, and it is not somebody's name.
# //   "GUN" (row 0) IS DEAD and is left alone: `cl_offer_name` intercepts kind 0
# //   and returns `g_cl_vw_name[arg]`, so this entry has never been drawn. Named
# //   rather than quietly deleted -- it is not this task's call to change a table
# //   length.
# let g_cl_off_name: array = [
#     "GUN", "SPARROW", "BRASS", "REPAIR",
#     "BELT", "REFIT", "BED", "PACK",
#     "SOUP", "TRIM", "LAMP KIT", "TRAVEL",
#     "WORK", "MAP", "ALMANAC", "CHARM"
# ];'''))
# 
# # V4  the horn banner contradicted the lantern the player had just lit
# CLIENT.append((
# '''        cl_banner(cl, "THE LAMPS ARE OUT");''',
# '''        // W7-VOICE: was "THE LAMPS ARE OUT", and it fired at the exact moment
#         // the player had just LIT one -- a banner contradicting the frame it is
#         // drawn over. It also repeated the intro card ("THE LAMPS WENT OUT ON A
#         // TUESDAY") sixty seconds later, meaning something else. What actually
#         // happened is the payoff to the warning under the light prompt, "IT'S
#         // GOING TO GET THEIR ATTENTION", so the banner is now that sentence
#         // coming true. 56 -> 44 triangles.
#         cl_banner(cl, "THEY HEARD IT");'''))
# 
# # V5  a toast that named nothing
# CLIENT.append((
# '''        cl_toast(cl, "A PATH OF LIGHT");''',
# '''        // W7-VOICE: was "A PATH OF LIGHT" -- a phrase, not a sentence, naming
#         // nothing the player can point at or act on. What the PATH beat does is
#         // put more unlit lanterns in the world. Same 48 triangles, one fact.
#         cl_toast(cl, "MORE LANTERNS");'''))
# 
# # V6  the card that promised a gun in a town that does not exist
# CLIENT.append((
# '''    cl_card(cl, "TINKER SMG - AT THE BENCH");
# }''',
# '''    // >>> W7-VOICE REMOVED A CARD HERE, AND THE REASON IS THAT IT WAS A LIE.
#     // <<<  It read "TINKER SMG - AT THE BENCH" and fired on the first lantern.
#     // Three things wrong with it, in order of severity:
#     //   1. THE BENCH IS MABEL'S, and at 0:30 there is no Village, no Mabel and
#     //      no bench. The player is told where to collect a gun roughly twenty
#     //      five minutes before the place exists.
#     //   2. THE TINKER IS A CITY GUN. weapons.hml's village row is
#     //      { BLACKTHORN, KESTREL, BELLOWS, NETTLE } and the Tinker is not in
#     //      it, so even once the bench exists this row is not on it.
#     //   3. THE BEAT ALREADY HAS A CARD. Lighting the lantern grants the FLARE,
#     //      and `cl_apply_w6`'s sp_have edge fires `cl_card(client_spell_name)`
#     //      on the same tick. Two cards competed for one 80-triangle slot and
#     //      the untrue one was drawn first.
#     // 80 triangles returned to the busiest moment in the first minute, and the
#     // card that survives says FLARE, which is a thing the player now has.
# }'''))
# 
# # V7  the first thing every villager ever says to you
# CLIENT.append((
# '''    if (t == 0) {                        // NPC_T_GREET
#         let s: string = "YOU CAME BACK.";
#         return s;
#     }''',
# '''    if (t == 0) {                        // NPC_T_GREET
#         // >>> "YOU CAME BACK." WAS SAID TO PEOPLE YOU HAD NEVER MET. <<<
#         // npc.hml 18 step 1 fires NPC_T_GREET on `met[w] == 0` and on nothing
#         // else -- it is the FIRST-MEETING topic, and it is the only topic that
#         // can never fire twice. Photographed: walk up to Mabel for the first
#         // time in your life and she says "You came back." It also spent the
#         // sentence this whole game is about ("the town survived because you
#         // came back", DIRECTION.md 6.4) on hello.
#         // Five lines now, one each, straight out of LORE.md 8's voices, all
#         // measured under the 96-triangle talk-line clamp (worst 88).
#         // "YOU CAME BACK." wants a RETURNING topic and npc.hml already tracks
#         // `talkday` per person, so the data is there; adding the topic needs an
#         // owner of both files and is REPORTED in docs/design/VOICE.md 6 rather
#         // than half-done here.
#         let g: i32 = clampi(a, 0, 4);
#         if (g == 0) { let s0: string = "YOU'LL BE THE LAMPLIGHTER."; return s0; }
#         if (g == 1) { let s1: string = "SIT DOWN. YOU'RE FILTHY."; return s1; }
#         if (g == 2) { let s2: string = "MM. YOU'RE ON THE ROTA."; return s2; }
#         if (g == 3) { let s3: string = "OH, MARVELLOUS. A NEW ONE."; return s3; }
#         let s4: string = "I KNEW SOMEONE WOULD COME.";
#         return s4;
#     }'''))
# 
# # V8  Wick's kit line, moving the seam main.hml declared
# CLIENT.append((
# '''    if (t == 8) {                        // NPC_T_NIGHT
#         let s: string = "STAY IN THE LIGHT TONIGHT.";
#         return s;
#     }''',
# '''    if (t == 8) {                        // NPC_T_NIGHT
#         let s: string = "STAY IN THE LIGHT TONIGHT.";
#         return s;
#     }
#     if (t == 10) {                       // NPC_T_KIT
#         // THE SEAM main.hml ASKED FOR, CLOSED. Its own comment: ">>> THIS
#         // BELONGS IN `client_talk_line` AS `if (t == 10)`, one line, the next
#         // time that file is owned. <<<" This is that time. `p_arg` is the ring
#         // in whole metres, so the line stays true as the town promotes and the
#         // ring goes 64 -> 120 -> 200. 76-80 of the 96 clamp.
#         // The space in "${a} M" is dropped: every other number on this HUD is
#         // "112M TO THE REACH", and three instruments saying the same word in
#         // the same shape is one lesson.
#         let s: string = `INSIDE ${a}M IT FEEDS US.`;
#         return s;
#     }'''))
# 
# # V9  thirty daily lines, because the sim had been selecting one of six since W6
# CLIENT.append((
# '''    // NPC_T_DAILY — the one line per in-game day, per person.
#     let w: i32 = clampi(p_who, 0, 4);
#     if (w == 0) { let s: string = "TWO FINGERS SHORT, STILL STRAIGHT."; return s; }
#     if (w == 1) { let s: string = "SOUP'S WARM PAST MIDNIGHT."; return s; }
#     if (w == 2) { let s: string = "MIND THE WICK, NOT THE FLAME."; return s; }
#     if (w == 3) { let s: string = "I'VE MAPPED WHAT I CAN SEE."; return s; }
#     let s2: string = "I FOUND A THING. HONEST.";
#     return s2;
# }''',
# '''    // ---- NPC_T_DAILY -----------------------------------------------------
#     //
#     // >>> `NPC_LINES = 6` HAS BEEN SELECTED, HASHED AND THROWN AWAY SINCE W6.
#     // <<<  npc.hml 18 step 10 computes a flavour index into `p_out[1]` and this
#     // arm read `p_who` and IGNORED `p_arg` entirely, so every villager had
#     // exactly ONE daily line, said on every in-game day, forever. GAME_DESIGN
#     // 5.7's "one new line per in-game day" has never once happened.
#     //
#     // AND ONE OF THE FIVE WAS ALSO TRUNCATING. Priced through the real font,
#     // "TWO FINGERS SHORT, STILL STRAIGHT." is 120 triangles against this draw
#     // site's 96-triangle clamp: `font_text_shadowed` cut it at 24 ink glyphs
#     // and Mabel's signature line rendered as "TWO FINGERS SHORT, STILL STR".
#     // Nobody had measured it because it took two in-game days and a Village to
#     // reach. Every one of the thirty below is under 96; the worst is 96/96.
#     //
#     // THE REPETITION IS THE POINT OF THIS FIX AND NOT THE COUNT. LORE.md 8 on
#     // Mabel's fingers: "She tells you this exactly once ... Do not soften her
#     // and do not give her a second scene about it. One scene. Then the
#     // weather." The shipping game told you every day. A line said too often is
#     // not merely wasted, it is RETROSPECTIVELY DAMAGED -- "Soup's warm past
#     // midnight" is the best sentence in this game exactly once.
#     //
#     // FORM: LORE.md 12.1 -- never about a quest, never about the player's
#     // stats, about the weather, food, a small worry, or a thing they noticed.
#     // Register: LORE.md 10 -- civic not epic, short sentences, ordinary words,
#     // understatement carries the grief. The style guide is docs/design/VOICE.md
#     // 5 and it is four rules long on purpose.
#     let w: i32 = clampi(p_who, 0, 4);
#     let d: i32 = clampi(a, 0, NPC_LINES_MAX - 1);
#     if (w == 0) {                        // MABEL -- gunsmith, dry, a tool she
#         // has repaired. Six about the gun and the weather; the fingers exactly
#         // once, in a form that fits (was 120 of 96, is 92).
#         if (d == 0) { let s: string = "WIND'S EAST. AIM LEFT."; return s; }
#         if (d == 1) { let s: string = "BRING IT BACK UNBROKEN."; return s; }
#         if (d == 2) { let s: string = "I'VE BURIED BETTER GUNS."; return s; }
#         if (d == 3) { let s: string = "THE SPRING'S TIRED. SO AM I."; return s; }
#         if (d == 4) { let s: string = "DON'T BLAME THE GUN."; return s; }
#         let s5: string = "TWO SHORT. STILL STRAIGHT.";
#         return s5;
#     }
#     if (w == 1) {                        // ODO -- everything is about food and
#         // none of it is about food. LORE.md 10.3 names "I kept a bowl" the
#         // saddest line in the game; it is here once, not daily.
#         if (d == 0) { let s: string = "SOUP'S WARM PAST MIDNIGHT."; return s; }
#         if (d == 1) { let s: string = "I KEPT A BOWL. I ALWAYS DO."; return s; }
#         if (d == 2) { let s: string = "YOU'VE GOT MUD ON YOU."; return s; }
#         if (d == 3) { let s: string = "THE BEDS TOOK BADLY. DRY."; return s; }
#         if (d == 4) { let s: string = "EAT SOMETHING. PLEASE."; return s; }
#         let s5: string = "SOUP KEEPS. THAT'S ALL.";
#         return s5;
#     }
#     if (w == 2) {                        // WICK -- aphorisms he refuses to
#         // explain, and the paperwork of an apocalypse that was a maintenance
#         // failure (LORE.md 2). He never once says what is at the bottom.
#         if (d == 0) { let s: string = "MIND THE WICK, NOT THE FLAME."; return s; }
#         if (d == 1) { let s: string = "MM. BACK BY THE SECOND HOUR."; return s; }
#         if (d == 2) { let s: string = "OIL WAS SHORT AGAIN."; return s; }
#         if (d == 3) { let s: string = "A LAMP DOESN'T ARGUE."; return s; }
#         if (d == 4) { let s: string = "FOUR HUNDRED YEARS OF IT."; return s; }
#         let s5: string = "MM. OFF YOU GO.";
#         return s5;
#     }
#     if (w == 3) {                        // CONNIE -- thrilled by danger she has
#         // personally never attended, and a bad writer in an endearing way.
#         if (d == 0) { let s: string = "I'VE MAPPED WHAT I CAN SEE."; return s; }
#         if (d == 1) { let s: string = "THE THIRD RING IS WRONG."; return s; }
#         if (d == 2) { let s: string = "REDREW IT TWICE. THRILLING."; return s; }
#         if (d == 3) { let s: string = "I'D COME. MY KNEE, THOUGH."; return s; }
#         if (d == 4) { let s: string = "SOMEONE MUST WRITE IT DOWN."; return s; }
#         let s5: string = "MY INK FROZE. AGAIN.";
#         return s5;
#     }
#     // PIP -- the lies get MORE specific the less true they are, and the last
#     // one is the ask LORE.md 8 says he makes once and you cannot grant.
#     if (d == 0) { let s: string = "I FOUND A THING. HONEST."; return s; }
#     if (d == 1) { let s: string = "I COULD CARRY MORE THAN YOU."; return s; }
#     if (d == 2) { let s: string = "I'VE GOT A SYSTEM."; return s; }
#     if (d == 3) { let s: string = "I'VE SEEN ONE. UP CLOSE."; return s; }
#     if (d == 4) { let s: string = "I SAID NOTHING. I AM BRAVE."; return s; }
#     let s2: string = "TAKE ME OUT THERE. YOU COULD.";
#     return s2;
# }'''))
# 
# # V9b  the local bound the daily table indexes against
# CLIENT.append((
# '''let g_cl_dir_name: array = ["NORTH", "NORTHEAST", "EAST", "SOUTHEAST",
#                             "SOUTH", "SOUTHWEST", "WEST", "NORTHWEST"];''',
# '''let g_cl_dir_name: array = ["NORTH", "NORTHEAST", "EAST", "SOUTHEAST",
#                             "SOUTH", "SOUTHWEST", "WEST", "NORTHWEST"];
# 
# // How many daily lines this file actually ships per villager. It MUST equal
# // `NPC_LINES` in src/sim/npc.hml, which is what the sim selects across -- if
# // this is smaller the top lines are unreachable, and if it is larger the clamp
# // below silently folds the extras onto the last one. It is a local constant and
# // not an import because the import wall runs the other way: the HUD may read
# // the sim, and this table is the HUD's.
# let NPC_LINES_MAX: i32 = 6;'''))
# 
# # --------------------------------------------------------------- main.hml
# # V10  the verdict pair
# MAIN.append((
# '''            g_l_verdict = "THE GRID TAKES IT";''',
# '''            // >>> W7-VOICE: WAS "THE GRID TAKES IT". <<<  Three faults, and
#             // the third is the one that decides it:
#             //   1. "GRID" APPEARS IN NO OTHER STRING IN THIS GAME. It is in
#             //      LORE.md 2 and in npc.hml's comments and nowhere a player can
#             //      read it, so the single most important confirmation in the
#             //      second verb was phrased in a noun nothing had taught.
#             //   2. "TAKES IT" reads as CONFISCATES at least as easily as it
#             //      reads as accepts. A player who plants a lantern and is told
#             //      something took it has been told they lost something.
#             //   3. THE FAILURE LINE WAS BETTER WRITTEN THAN THE SUCCESS LINE.
#             //      The other arm is "25M OUTSIDE THE REACH" -- concrete,
#             //      numeric, in the taught vocabulary -- and the reward was an
#             //      abstraction. That is exactly backwards.
#             // "THE TOWN GETS IT" names the beneficiary, needs no new word, and
#             // is what the fantasy is actually about. 56 -> 52 triangles.
#             g_l_verdict = "THE TOWN GETS IT";'''))
# 
# MAIN.append((
# '''            g_l_verdict = `${gap}M OUTSIDE THE REACH`;''',
# '''            // W7-VOICE, and this one is only visible in the FRAME. At the
#             // moment this draws, the prompt 40 px below it reads "25M TO THE
#             // REACH" -- the same number, the same fact, two prepositions,
#             // photographed together in scratchpad/vgrid/ns_33. "TO" and
#             // "OUTSIDE" read as a contradiction at 320x180 even though both are
#             // true. "SHORT OF" is a verdict rather than a second measurement,
#             // keeps the taught noun, and costs the same 72 triangles.
#             g_l_verdict = `${gap}M SHORT OF THE REACH`;'''))
# 
# # V11  the wave readout's multiplication sign
# MAIN.append((
# '''    if (ws != DIR_IDLE && used + 44 <= cap) {
#         used = used + hud_text(4.0, 16.0,
#                                `WAVE ${client_wave(g_cl) + 1}  x${client_alive(g_cl)}`,
#                                g_UI_DIM, clampi(cap - used, 0, 44));
#     }''',
# '''    // W7-VOICE: was `WAVE 1  x0`, unconditionally. `x0` is a COUNT rendered
#     // as a MULTIPLIER -- "wave one, times zero" -- and it sat on screen reading
#     // zero for the whole gap between waves, which is the one state in which it
#     // has nothing to say. It now says nothing when the answer is none.
#     //
#     // >>> AND IT IS THE CHEAP VERSION ON PURPOSE. <<<  The first draft of this
#     // edit read `WAVE 1  3 LEFT`, which is better English and costs 40
#     // triangles against this line's 24. Measured on a real `--demo 80`, that
#     // took the HUD peak 332 -> 354 of a 360 cap: SIXTEEN of the twenty-eight
#     // triangles left in the whole HUD, spent turning "x3" into "3 LEFT" on a
#     // line nobody has ever complained about. CLAUDE.md 3 is a hard rule and an
#     // anti-cringe pass does not get to spend the frame budget on taste. The
#     // version below is <= the old cost in EVERY state and still kills the `x0`.
#     if (ws != DIR_IDLE && used + 44 <= cap) {
#         let alive: i32 = client_alive(g_cl);
#         let wl: string = `WAVE ${client_wave(g_cl) + 1}`;
#         if (alive > 0) { wl = `WAVE ${client_wave(g_cl) + 1}  x${alive}`; }
#         used = used + hud_text(4.0, 16.0, wl,
#                                g_UI_DIM, clampi(cap - used, 0, 44));
#     }'''))
# 
# # V12  the character sheet showed three-letter stubs in a panel with 250 px spare
# MAIN.append((
# '''        n = n + hud_panel_text(34.0, g_PANEL_Y + 8.0, "CHARACTER", g_UI_AMBER,''',
# '''        // W7-VOICE: was "CHARACTER", while the F1 controls card calls the same
#         // panel "SHEET". One thing, two names, exactly the defect the trade
#         // panel had with PUMP/BELLOWS. Both now say STATS, which is also what
#         // the five rows below are. 36 -> 20 triangles.
#         n = n + hud_panel_text(34.0, g_PANEL_Y + 8.0, "STATS", g_UI_AMBER,'''))
# 
# MAIN.append((
# '''        n = n + hud_help_row(4, "TAB", "SHEET", p_budget - n);''',
# '''        n = n + hud_help_row(4, "TAB", "STATS", p_budget - n);'''))
# 
# # V13  the version banner still says W6
# MAIN.append((
# '''let g_VERSION: string = "NIGHTSHADE W6 - the first minute, and the town you carry it home to";''',
# '''// W7-VOICE: the wave tag came out. It was two waves stale, it is an internal
# // build id in a line a player reads at boot, and the subtitle is the best
# // one-sentence statement of this game that exists anywhere in the repo.
# let g_VERSION: string = "NIGHTSHADE - the first minute, and the town you carry it home to";'''))
# 
# # V15  the character panel rows
# MAIN.append((
# '''            n = n + hud_panel_row(g_PANEL_Y + 26.0 + f64(i) * 14.0, sel,
#                                   client_stat_short(i),''',
# '''            // W7-VOICE: was `client_stat_short(i)` -- HND / CAP / LAN / VIT
#             // / ATT, five three-letter stubs in a panel with 250 px of empty
#             // room to the right of every one of them (photographed,
#             // scratchpad/vpanel/ns_23). "LAN" is not a word. The full table
#             // ALREADY EXISTED in client.hml and nothing was calling it.
#             // Measured through the real font: the five labels go 60 -> 164
#             // triangles, taking the panel 212 -> 316 of the 360 cap, which
#             // still clears probe_hud's 36-triangle headroom rule at 44. It does
#             // make probe_hud's `g_PANEL_CHAR = 212` stale, and that file is not
#             // this task's -- see docs/design/VOICE.md 6.
#             n = n + hud_panel_row(g_PANEL_Y + 26.0 + f64(i) * 14.0, sel,
#                                   client_stat_name(i),'''))
# 
# 
# # V16  --scene hud_worst_case hardcodes the strings it prices. If they change
# #      and it does not, the worst-case harness is measuring a string the game
# #      no longer contains -- exactly the stale-literal class CLAUDE.md warns of.
# MAIN.append((
# '''    if (hw_cost_big("THE LAMPS ARE OUT") > hw_cost_big(g_hw_banner)) {
#         g_hw_banner = "THE LAMPS ARE OUT";
#     }
#     g_hw_toast = "A PATH OF LIGHT";''',
# '''    if (hw_cost_big("THEY HEARD IT") > hw_cost_big(g_hw_banner)) {
#         g_hw_banner = "THEY HEARD IT";
#     }
#     g_hw_toast = "MORE LANTERNS";'''))
# 
# MAIN.append((
# '''    g_hw_verdict = "999M OUTSIDE THE REACH";
#     if (hw_cost_micro("THE GRID TAKES IT") > hw_cost_micro(g_hw_verdict)) {
#         g_hw_verdict = "THE GRID TAKES IT";
#     }''',
# '''    g_hw_verdict = "999M SHORT OF THE REACH";
#     if (hw_cost_micro("THE TOWN GETS IT") > hw_cost_micro(g_hw_verdict)) {
#         g_hw_verdict = "THE TOWN GETS IT";
#     }'''))
# 
# 
# # V17  the apostrophe glyph costs the game's best teaching line four triangles.
# #      Found by probe_hud section H going red -- 112 > 108 -- on the FIRST run
# #      after the hudgen fix, which is the probe doing exactly its job.
# MAIN.append((
# '''let g_TB_PROMPT: i32 = 108;''',
# '''// W7-VOICE: 108 -> 112. `IT'S GOING TO GET THEIR ATTENTION` cost 108 with an
# // apostrophe that drew nothing, which is to say it was sitting EXACTLY on its
# // clamp only because a glyph was missing. Give FONT_MICRO the apostrophe (see
# // src/art/hudgen.hml) and the line is 112, and `font_text_shadowed` would drop
# // its last glyph on every lantern hold in the game. probe_hud section H caught
# // it on the first run. Budgets must be multiples of 4 to be spent exactly (see
# // g_TB_CARD above); 112 is, and it is the string's whole cost.
# // Measured: HUD peak over `--demo 80 --seed 1337` is 330 of 360 WITH this and
# // the apostrophe, against 334 without either, so it is paid for several times
# // over by the rest of this pass.
# let g_TB_PROMPT: i32 = 112;'''))
# 
# 
# def apply(path, edits, label):
#     with open(path) as f:
#         s = f.read()
#     n = 0
#     for old, new in edits:
#         c = s.count(old)
#         if c != 1:
#             print(f"  !! {label}: expected 1 occurrence, found {c}:\n     {old.splitlines()[0][:78]}")
#             sys.exit(2)
#         s = s.replace(old, new)
#         n += 1
#     with open(path, "w") as f:
#         f.write(s)
#     print(f"  {label}: {n} replacements applied")
# 
# 
# apply(os.path.join(ROOT, "src/game/client.hml"), CLIENT, "client.hml")
# apply(os.path.join(ROOT, "src/game/main.hml"), MAIN, "main.hml")
# print("ok")
